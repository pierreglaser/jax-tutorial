## A set of `jax`-related utilities.


Right now, this repository only contains a small function that sets up the right
environment variables to get `jax` running against a `cuda` toolkit installed using
`conda`
